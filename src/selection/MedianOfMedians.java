package selection;

//TODO
public class MedianOfMedians {

	//Calculate pivot as median element and then use quickselect
	//Split large array to small arrays
	//sort each small array with insertion sort, O(N) complexity in small array
	//take median of each subarray
	//sort all median values and get a median of median
	//median values is pivot element
}
